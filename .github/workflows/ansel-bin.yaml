---
# yamllint disable rule:line-length
name: Ansel-bin copr rebuild
on:
  push:
    branches:
      - main
    paths:
      - tools-misc/ansel-bin/ansel-bin.spec
  workflow_dispatch:

jobs:
  curl:
    runs-on: ubuntu-latest
    steps:
      - name: Run webhook for copr
        run: |
          curl -X POST https://copr.fedorainfracloud.org/webhooks/custom/197880/c61ad174-3b66-4910-b4fb-8d33b0b9abdb/ansel-bin
