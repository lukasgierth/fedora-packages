---
# yamllint disable rule:line-length
name: kwin-dynamic-workspaces copr rebuild
on:
  push:
    branches:
      - main
    paths:
      - tools-misc/kwin-dynamic-workspaces/kwin-dynamic-workspaces.spec
  workflow_dispatch:

jobs:
  curl:
    runs-on: ubuntu-latest
    steps:
      - name: Run webhook for copr
        run: |
          curl -X POST https://copr.fedorainfracloud.org/webhooks/custom/${{ secrets.COPR_TOOLS_MISC }}/kwin-dynamic-workspaces
