---
# yamllint disable rule:line-length
name: lazysql copr rebuild
on:
  push:
    branches:
      - main
    paths:
      - tools-golang/lazysql/lazysql.spec
  workflow_dispatch:

jobs:
  curl:
    runs-on: ubuntu-slim
    steps:
      - name: Run webhook for copr
        run: |
          curl -X POST https://copr.fedorainfracloud.org/webhooks/custom/${{ secrets.COPR_TOOLS_GOLANG }}/lazysql
