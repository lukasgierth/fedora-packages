---
# yamllint disable rule:line-length
name: netscanner copr rebuild
on:
  push:
    branches:
      - main
    paths:
      - tools-rust/netscanner/netscanner.spec
  workflow_dispatch:

jobs:
  curl:
    runs-on: ubuntu-latest
    steps:
      - name: Run webhook for copr
        run: |
          curl -X POST https://copr.fedorainfracloud.org/webhooks/custom/${{ secrets.COPR_TOOLS_RUST }}/netscanner
